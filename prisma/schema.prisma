// Prisma schema placeholder — full schema will be implemented in Phase 2.
// This file exists to establish the correct monorepo structure.
//
// Phase 2 will implement:
// - All 8 entities: Tenant, IndustryConfig, User, Conversation, Message,
//   ExtractedData, Lead, LeadAssignment
// - All relations, indexes, and cascade rules
// - Row-Level Security policies (Phase 3)

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
